---
// Heading component for consistent h2 styling in BioLink
interface Props {
  text: string;
}

const { text } = Astro.props;
---

<h2 class="font-heading text-xl font-bold mb-4 text-center">{text}</h2> 